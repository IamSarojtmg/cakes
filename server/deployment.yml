apiVersion: apps/v1
kind: Deployment
metadata:
  name: cakes-nodeapp-deployment
  labels:
    app: nodeapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodeapp
  template:
    metadata:
      labels:
        app: nodeapp 
    spec:
      containers:
      - name: nodeserver
        image: saroj777/cakesbackend:latest
        ports:
        - containerPort: 3001